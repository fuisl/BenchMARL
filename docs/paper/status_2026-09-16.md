# Status report — 2026-09-16

**Same-day review, and the repairs that followed.**
[`review_2026-09-16.md`](review_2026-09-16.md) reproduced three defects not
accounted for in the first version of this report: Balance's heuristic
trajectories were generated by Transport's policy, the horizon diagnostic rolled
through a positional slot with no prediction training, and the closed loop had no
Balance outcome contract. All three are fixed in commit `43432b1`, with contract
checks in `test/test_world_model_contracts.py`. **§3 has been rewritten** against
the corrected measurement (job 1235); §§5–8 still carry the narrowing the review
asks for and should be read alongside it.
[Review evidence](../../outputs/review_20260916/evidence.json) ·
[corrected horizon tables](../../outputs/horizon_rescore_1235/)

Where the project stands after jobs 1217–1233, what each result actually
supports, and what is still unresolved. Written because the results had spread
across `outputs/`, wandb and terminal scrollback with no single place to look.

Companion documents: [`outline.md`](outline.md) decides which paper gets written;
[`audit_2026-09-15.md`](audit_2026-09-15.md) records what was wrong with the
measurements; [`experiments/README.md`](experiments/README.md) maps every job to
its artifacts. **Deadlines: abstract 1 Oct (15 days), full paper 8 Oct (22 days).**

---

## 1. The claim, link by link

The project's bet: a world model that explicitly represents *"what agent A does
changes what happens to agent B"* will predict better, rank plans better, and
control better than models that do not.

| Link | Verdict | Confidence |
|---|---|---|
| 1. Predicting unseen action combinations | **Works on Buzz Wire, replicates on a second bank. Does not replicate on Balance.** | Medium — was High before 1233 |
| 2. Better plan ranking | **Task-dependent, and the architecture that wins flips between tasks** | Low |
| 3. Better control | **Not measured.** The objective used for three months was task-orthogonal | High that it is unmeasured |
| 4. Benefit tracks physical coupling | **Weakened.** Two of four tasks have no measurable coupling at all | Low |

Links 3 and 4 moved this week, both downward, both for measurement reasons rather
than model reasons.

## 2. Link 1 — counterfactual prediction

Intervention response, **lower is better**, 1.0 = no better than predicting no
change at all. Eight seeds, paired on identical anchors.

| Task | Bank | joint | relational | Active anchors |
|---|---|---:|---:|---:|
| Buzz Wire | dev, λ=0.09 | 0.766 / 0.870 | **0.731 / 0.739** | 113 / 117 |
| Buzz Wire | dev, λ=0.009 | 0.781 / 0.900 | **0.650 / 0.741** | 113 / 117 |
| Buzz Wire | holdout | 0.327 / 0.427 *(ρ)* | **0.479 / 0.470** *(ρ)* | 110 / 115 |
| Transport | dev | 0.088 / 0.107 *(ρ)* | 0.063 / 0.020 *(ρ)* | **31 / 239** |
| Dropout | dev | — | — | **0 / 160** |
| Wheel | dev | — | — | **0 / 160** |
| Balance | dev | 1.696 / 2.105 | 1.185 / 1.267 | 113 / 197 |

Read the two failures in that table before the successes. **Dropout and Wheel
have zero interaction-active anchors** — their C7 is not a null result, it is
unmeasurable, and the paper must say so rather than reporting a null. **Transport
has 31 of 239**, which is why its numbers hover near zero for every model.

So link 1's dose-response claim rests on **Buzz Wire plus its holdout
replication**, and now has a contradicting case in Balance where both conditioned
models score **above 1.0** — worse than assuming no interaction.

**The regulariser gate (job 1228).** Every number in this project before 1228
used LeWM's default SIGReg weight of 0.09. At dim 192, dropping to 0.009 widens
the correlated relational-vs-joint gap from 0.035 to **0.131** and lowers rollout
error ~40% for all three models. Below dim 192, `joint` captures *nothing* at any
setting — the whole C7 result exists only at the largest width run.

## 3. Rollout error against horizon — corrected, and the cap was an artefact

Full tables: [`experiments/09_horizon_rollout.md`](experiments/09_horizon_rollout.md),
rescored as job **1235** after the review. **This section replaces the version
written earlier the same day**, which reported a cliff and an architectural cap.
Both were the evaluator rolling through a positional slot that never receives a
prediction gradient: training predicts from `latent[:, :-1]`, so five of the six
allocated positions are supervised and position 5 is only ever decayed.

Transport, one block = 5 primitive steps, corrected sliding window:

| predictor | h=1 | h=3 | h=5 | h=8 | h=15 |
|---|---:|---:|---:|---:|---:|
| independent (single-agent) | 0.0176 | 0.0323 | 0.0355 | 0.0411 | 0.0526 |
| joint | 0.0176 | 0.0272 | 0.0304 | 0.0357 | 0.0474 |
| relational | **0.0171** | **0.0252** | **0.0284** | **0.0325** | **0.0430** |

What this settles:

1. **The multi-agent advantage is a compounding effect, not a per-step one.** At
   h=1 the three predictors are indistinguishable and `joint` is marginally
   *worse*. The relational margin grows 14× between h=1 and h=5, and keeps
   growing to +0.0096 at h=15. Reporting `one_step_error` alone would miss it.
2. **The relational ordering holds at every horizon measured** on Transport and
   Buzz Wire, in both data regimes — not only inside the trained window.
3. **Transport is at 4.6% of latent variance at 75 primitive steps** (variance
   0.9258). There is no point in this range where prediction becomes useless.
4. **Balance reverses the short-horizon ordering**: `joint` beats `relational` at
   h≤5 in both regimes. The heuristic branch is not involved in these rows, so
   the source-policy defect does not explain it. Same direction as Balance's
   plan-ranking result — see §6.

**Withdrawn from the earlier version of this section:** the 18.7× cliff at
h=5→h=8; the claim that the single-agent model is best at h≥8; the claim that
every model is past latent variance by h=10; and the consequence that lookahead
past 25 primitive steps requires recollecting longer snippets.

## 4. Link 3 — control was never measured

Full account: [`experiments/10_goal_objective.md`](experiments/10_goal_objective.md).
Every planner below uses the **true simulator** as its dynamics, so none of it is
a statement about a learned model.

| Design | Result |
|---|---|
| Random-plan goal endpoint (1218/1221/1226) | Goal oracle reaches it **20/20 on both tasks** and ends at the task distance random reaches (0.8993 vs 0.8988 Transport; 1.0937 vs 1.0852 Buzz Wire). **Orthogonal to the task** |
| Best-of-300 by task distance | Improves on candidate 0 by **0.0012** on Transport. Random actions make no task progress. Discarded |
| Competent-controller goal (1229) | Goal oracle collides **90%** vs random's 70%, finishes worse than doing nothing. Cancelled |
| Velocity dropped from the metric (1230) | 0.9841 task, 0.85 collisions. Unchanged. **Rejected** |
| Goals from solved episodes only (1231) | Beats random and do-nothing on the task (0.3658 vs 0.5029/0.5069), **2/20 native successes — the first ever**. Still 85% collisions. **Partial pass** |

**Consequence:** the claim that learned planners "close 30% (Buzz Wire) / 41%
(Transport) of the random → goal-oracle gap" is **withdrawn**. The gap is real;
crossing it changes nothing about the task.

**The mechanism, which is publishable on its own.** Buzz Wire agents observe
`[own pos, own vel, own pos − own goal]`. They never observe the ball, and never
observe the wire. A goal in that space can say *where the agents should be*; it
cannot say *do not let the ball touch the wire*, because no term in the objective
changes as the ball nears a wall. Reward planning sees the collision penalty;
goal planning is structurally blind to it. On identical states the reward oracle
scores **17/20** and the goal oracle **2/20**.

This generalises past this project: any observation-space goal on a task whose
failure mode is unobserved carries this hazard, and the diagnostic is cheap —
score the goal oracle on the task's own metric and its collision rate, not only
on goal distance.

**Do-nothing beats random on goal distance in every table above.** That baseline
is the audit's contract item 5; it had never been run, and it would have caught
the original design on day one.

## 5. Stage 2 and Balance agree: structure substitutes for information

**Stage 2 (job 1223)** — 144 runs, Buzz Wire, three inputs × three predictors ×
eight seeds. Rollout error, best cell per input:

| input | best | joint vs relational, paired |
|---|---|---|
| observation (6 dims) | 0.2429 relational | relational wins **7/8, 7/8** |
| history (18 dims) | 0.2315 relational | relational wins **6/8, 8/8** |
| physical (24 dims) | **0.1888 joint** | **joint** wins 6/8, **8/8** (−0.0543, p≈0.008) |

Information helps by 22% (0.2429 → 0.1888), and the architecture ordering
**reverses** once the hidden state is supplied.

**Balance (job 1233)** points the same way by a different route. Balance observes
the package position, its goal offset, its velocity and the full line state
natively — it is closer to the "information present" condition, arrived at by
changing the task rather than the input. Plan ranking over a fixed random
candidate bank, absolute and paired against `independent`:

```
independent regime   spearman   regret    paired vs independent
  independent          0.1978   4.3748    --
  joint                0.2842   3.5318    spearman +0.0863 8/8, regret -0.843 8/8
  relational           0.2113   4.2330    spearman +0.0135 6/8, regret -0.142 4/8

correlated regime
  independent          0.2272   4.3200    --
  joint                0.2347   4.1228    spearman +0.0075 6/8, regret -0.197 5/8
  relational           0.2255   4.6172    spearman -0.0017 3/8, regret +0.297 4/8
```

Joint wins the independent regime cleanly. In the correlated regime nothing
separates — joint's margin is +0.0075 with a CI crossing zero. So this is one
regime on one bank, not a task-level result.

**Now a third route, from the corrected horizon tables (job 1235):** on Balance,
`joint` beats `relational` at h≤5 in **both** regimes. That table uses only the
correlated/independent branches, so the source-policy defect does not touch it.

**The hypothesis all three point at:** *relational structure compensates for
unobservable coupling, and stops paying — or reverses — once the coupling
variable is observed.* It is a hypothesis, not an identified mechanism. The
review is right that Balance changes physics, agent count, data and objective as
well as observability, so it is not a controlled observation intervention; and
that Stage 2's 22% compares separately learned latent errors rather than physical
prediction. The clean test is a **within-task** observability toggle followed
through to decision quality — `give_way`'s `observe_rel_pos` is exactly that.

## 6. The unresolved tension

On Balance's **same checkpoints**, three metrics disagree:

| Metric | Winner |
|---|---|
| Counterfactual prediction error, paired | **relational** (6/8, 7/8 seeds) |
| Intervention response ratio | **neither** — both above 1.0 |
| Stratified concentration on active anchors | **relational** (−2.8 points, CI [−4.2, −1.3]) |
| Plan ranking Spearman and regret | **joint** (8/8 seeds) |

This must be resolved before anything is written. Relational predicts the
counterfactual *state* better and its advantage concentrates where interaction
happens, yet it fails to capture the interaction *response* and does not convert
to plan ranking. Leading with any one of these without the others would be
selective reporting.

**The review narrows the tension itself, and it is partly a comparison
mismatch.** Three things to hold onto:

* The raw active-anchor counterfactual errors are **lower for joint** (correlated
  0.34231 vs 0.36768; independent 0.35441 vs 0.38586). Relational only wins the
  *paired-against-independent* table — and each term there is an error in its own
  model's latent space, so that difference is a within-pair sign test, not a
  physical margin. Both tables now say so in their own output.
* The "shared physical probe" correlates response **magnitude** with observation
  **magnitude**. It decodes no physical vector and checks no direction, so a
  prediction pointing the wrong way can rank perfectly.
* These metrics use different encoders, targets, horizons and populations from
  the ranking experiment. There is no mathematical requirement that they agree.

So the resolution is not to pick a winner among them: it is one comparison on the
**same anchors, candidates, horizon, physical outputs and task cost** throughout.
That is Gate 2 of [`review_2026-09-16.md`](review_2026-09-16.md) §7.

## 7. Task suite health

Two of the four original tasks cannot support the claims they were chosen for.

| Task | Status |
|---|---|
| Buzz Wire | Agents observe neither the ball nor the wire. Goal objective structurally blind to the failure mode. Coupling measurable (113/117 anchors) |
| Transport | **Uncontrollable from its anchors** — VMAS's own heuristic closes 0.032 of the starting 0.9006 across a full 100-step episode; doing nothing loses 0.0012. Its universal 0/20 native success is not evidence about any model. Coupling barely measurable (31/239) |
| Dropout | **0 interaction-active anchors.** C7 unmeasurable |
| Wheel | **0 interaction-active anchors.** C7 unmeasurable |
| **Balance** *(job 1233)* | Task state observed (package, goal offset, velocity, line); **113/197 anchors active**. The best task in the suite, but its bank's heuristic branch is Transport's policy ([DEFECT.md](../../outputs/balance_1233/DEFECT.md)), so its coverage argument is withdrawn and it needs recollecting |

Six VMAS candidates were probed over 64 episodes before Balance was chosen. The
review adds three corrections to that selection:

* **`navigation`** needs both `cvxpy` **and `cvxpylayers`**; installing only
  `cvxpy` as planned here is insufficient. Its lidar is range-limited sensing,
  not observation of other agents' states or actions.
* **`give_way`** also exposes other-agent position, through `observe_rel_pos`,
  and that flag is a **within-task** toggle — the controlled observability
  experiment §5 actually needs, which no cross-task comparison can give.
* **Wheel's and Dropout's zero active anchors** are one agent, one action axis,
  the selected anchors and the measured horizon. That is not a task-wide absence
  of interaction, and Dropout's shared reward/termination may couple the decision
  problem even where the physics does not.

## 8. What I would do next, in order

Reordered to follow [`review_2026-09-16.md`](review_2026-09-16.md) §7, whose gate
structure supersedes the list written earlier today.

1. **Gate 0 — done.** Policy dispatch, Balance outcome contract, horizon context
   and execution cadence are repaired in `43432b1`; the horizon curve is rescored
   (job 1235); the old Balance bank is labelled. What remains of Gate 0 is a
   fresh Balance collection on the fixed collector.
2. **Gate 1 — make Balance controllable with true dynamics.** 32–64 development
   roots; zero / random / the correct heuristic / simulator MPC; H=5 fixed,
   execute-5 versus execute-1 (`--execute-blocks` now exists). Native reward
   objective first. *If the heuristic works and simulator MPC does not, the
   problem is search and objective, not the world model.*
3. **Gate 2 — one common-coordinate comparison** on the same anchors, candidates,
   horizon and cost. This is what resolves §6; picking among the current metrics
   does not.
4. **Write the nine missing job notes.** [`experiments/README.md`](experiments/README.md)
   lists them.
5. **Decide the paper.** [`outline.md`](outline.md) §5 recommends the diagnosis
   paper. That recommendation is now weaker in one specific way: a diagnosis
   paper built on an artificial horizon cliff and a mislabelled competent bank
   would repeat the failure it describes. The durable version is the controlled
   link from intervention coverage → physical response → candidate regret →
   native control, including the case where joint is simply sufficient.

## 9. What must not be claimed

* That learned planners achieve any measure of control. They do not, and until
  1231 the objective could not have shown it either way.
* That the relational advantage is general. It holds on Buzz Wire, replicates on
  its holdout, and **fails to replicate on Balance**.
* That coupling dose-response holds across four tasks. Two of the four have no
  measurable coupling.
* Any C7 number from before the F6 repair (job 1224). Rollout-horizon numbers
  past h=5 are extrapolation — the model was never asked for a sixth consecutive
  prediction in training — but they are no longer meaningless: the earlier
  version of that caveat described an evaluator defect, not a model limit (§3).
* That job 1233's Balance bank contains competent Balance behaviour. Its
  heuristic branch is Transport's policy; see
  [`review_2026-09-16.md`](review_2026-09-16.md) §2 and
  [`../../outputs/balance_1233/DEFECT.md`](../../outputs/balance_1233/DEFECT.md).
